@model IEnumerable<ProyectoP1Web2.Models.HorarioGimnasio_Horas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HorarioGimnasio.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Horas.IdHora)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HorarioGimnasio.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Horas.IdHora)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idHorarioGimnasio_Horas }) |
            @Html.ActionLink("Details", "Details", new { id=item.idHorarioGimnasio_Horas }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idHorarioGimnasio_Horas })
        </td>
    </tr>
}

</table>
