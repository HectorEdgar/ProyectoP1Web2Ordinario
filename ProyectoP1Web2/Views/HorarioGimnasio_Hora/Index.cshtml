@model IEnumerable<ProyectoP1Web2.Models.HorarioGimnasio_Hora>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Agregar nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hora.IdHora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HorarioGimnasio.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hora.IdHora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HorarioGimnasio.nombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idHorarioGimnasio_Horas }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idHorarioGimnasio_Horas }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.idHorarioGimnasio_Horas })
        </td>
    </tr>
}

</table>
