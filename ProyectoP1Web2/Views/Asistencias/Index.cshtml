@model IEnumerable<ProyectoP1Web2.Models.Asistencia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Agregar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Alumnos_GrupoGimnasio.Matricula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Alumnos_GrupoGimnasio.Matricula)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idAsistencia }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idAsistencia }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.idAsistencia })
        </td>
    </tr>
}

</table>
